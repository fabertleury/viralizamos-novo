import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Comprar Visualizações Reels Por 1 Real - Entrega Imediata',
  description: 'Aumente as visualizações dos seus Reels por apenas R$1. Entrega instantânea e real impulsionamento para o seu perfil com a Viralizamos.',
};

export default function VisualizacoesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 