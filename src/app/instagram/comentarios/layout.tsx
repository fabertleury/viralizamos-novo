import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Comprar Comentários Instagram Brasileiros - Envio Imediato',
  description: 'Gere engajamento com comentários brasileiros reais no Instagram. Envio imediato e opções personalizadas para seus posts com a Viralizamos.',
};

export default function ComentariosLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 